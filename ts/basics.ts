export let house: string;
export let homeNumber: number;
export let isHome: boolean;
export let homeArray: Array<string>;
export let adresArray: Array<number | string>;

export const home2Array: Array<string> = ['home', 'street', 'city'];

const dotCom:string = 'https://www.example.com';
const reducer:boolean = true;

